# 模型知识规则审核清单

- [x] 来源包记录模型、知识版本、置信度、审核状态和变更原因。
- [x] 神煞、建除和宜忌使用独立稳定 ID，不使用数量评分。
- [x] 可比冲突声明正整数 `priority`，低数值优先；不可比差异并列保留。
- [x] 正式规则置信度不低于 `0.8` 且状态为 `approved`。
- [x] 黄金案例与 `tyme4ts@1.5.2` 公开 API 完成逐字段对拍。
- [x] 金符经保持 `experimental`，不进入默认正式规则集。
- [x] 规则包、来源包、允许差异和发布清单均保留历史版本，不覆盖旧文件。
